spring:
  application:
    name: moamart

  profiles:
    active: dev

  jpa:
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
      enabled: true